const dynamicRoadmapService = require('./services/dynamicRoadmapService');
require('dotenv').config();

async function testMobileRoadmap() {
  console.log('🚀 Testing Mobile Development Roadmap Generation...\n');
  
  try {
    const roadmap = await dynamicRoadmapService.generateRoadmap('Mobile Development', 'beginner', '3-6 months');
    
    console.log('Generated by:', roadmap.generatedBy || 'Unknown');
    console.log('Success:', roadmap.success);
    
    if (roadmap.success && roadmap.data) {
      console.log('\n📱 MOBILE DEVELOPMENT ROADMAP:');
      console.log('='*60);
      console.log('Title:', roadmap.data.title);
      console.log('Skill:', roadmap.data.skill);
      console.log('Level:', roadmap.data.level);
      console.log('Duration:', roadmap.data.totalDuration);
      console.log('Total Chapters:', roadmap.data.chapters.length);
      
      console.log('\n📚 CHAPTERS:');
      roadmap.data.chapters.forEach((chapter, index) => {
        console.log(`\n${index + 1}. ${chapter.title}`);
        console.log(`   Description: ${chapter.description}`);
        console.log(`   Duration: ${chapter.duration}`);
        console.log(`   Topics: ${chapter.topics.join(', ')}`);
        console.log(`   Projects: ${chapter.practicalProjects.join(', ')}`);
      });
      
      console.log('\n💼 CAREER OUTCOMES:');
      roadmap.data.careerOutcomes.forEach(outcome => {
        console.log(`   - ${outcome}`);
      });
      
      console.log('\n💰 SALARY RANGE:', roadmap.data.estimatedSalaryRange);
    }
  } catch (error) {
    console.error('Error:', error.message);
  }
  
  console.log('\n' + '='*60);
  console.log('Now testing JavaScript roadmap to compare...\n');
  
  try {
    const jsRoadmap = await dynamicRoadmapService.generateRoadmap('JavaScript', 'intermediate', '2-4 months');
    
    if (jsRoadmap.success && jsRoadmap.data) {
      console.log('📘 JAVASCRIPT ROADMAP:');
      console.log('Title:', jsRoadmap.data.title);
      console.log('First Chapter:', jsRoadmap.data.chapters[0]?.title);
      console.log('Topics in First Chapter:', jsRoadmap.data.chapters[0]?.topics);
    }
  } catch (error) {
    console.error('Error:', error.message);
  }
}

testMobileRoadmap();
